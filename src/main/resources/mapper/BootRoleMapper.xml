<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sg.vue.dao.BootRoleMapper" >
  <resultMap id="BaseResultMap" type="com.sg.vue.role.BootRole" >
    <id column="role_id" property="roleId" jdbcType="INTEGER" />
    <result column="user_id" property="userId" jdbcType="INTEGER" />
    <result column="url_id" property="urlId" jdbcType="VARCHAR" />
  </resultMap>

  <sql id="Base_Column_List" >
    role_id, user_id, url_id
  </sql>

  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    select 
    <include refid="Base_Column_List" />
    from boot_role
    where role_id = #{roleId,jdbcType=INTEGER}
  </select>

  <select id="selectRoleByUserId" resultType="com.sg.vue.role.model.BootRoleResultVO" parameterType="java.lang.Integer" >
    SELECT
      c.id "urlId",
      c.config_value "urlValue"
    FROM
      boot_role r
        JOIN boot_config c ON r.url_id = c.id
        AND c.flag = '01'
    where r.user_id = #{userId,jdbcType=INTEGER}
  </select>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    delete from boot_role
    where role_id = #{roleId,jdbcType=INTEGER}
  </delete>

  <insert id="insertSelective" parameterType="com.sg.vue.role.BootRole" >
    insert into boot_role
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="roleId != null" >
        role_id,
      </if>
      <if test="userId != null" >
        user_id,
      </if>
      <if test="urlId != null" >
        url_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="roleId != null" >
        #{roleId,jdbcType=INTEGER},
      </if>
      <if test="userId != null" >
        #{userId,jdbcType=INTEGER},
      </if>
      <if test="urlId != null" >
        #{urlId,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.sg.vue.role.BootRole" >
    update boot_role
    <set >
      <if test="userId != null" >
        user_id = #{userId,jdbcType=INTEGER},
      </if>
      <if test="urlId != null" >
        url_id = #{urlId,jdbcType=VARCHAR},
      </if>
    </set>
    where role_id = #{roleId,jdbcType=INTEGER}
  </update>

</mapper>